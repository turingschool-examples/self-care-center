


function getRandomIndex(array) {
  return Math.floor(Math.random() * array.length);
}

function selectMessage(){
  //if input is "affirmation" display random affirmation
  //else display random mantra
}
